name: Ping Every 5 Minutes

on:
  schedule:
    - cron: '*/5 * * * *' # 每5分钟执行一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Send GET Request
      uses: wei/curl@master
      with:
        args: '-X GET https://qwen-free-api-1kmu.onrender.com/ping'
